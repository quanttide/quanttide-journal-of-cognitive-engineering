我们这套方案可以被称为 harness process。我们充分利用模型可能会犯的错误，通过语义层面去约束它，制定一些有效的流程或评估标准。这样，整个系统哪怕没有 AI，只要能跟外部系统的 AI 对接上，并且过程数据和成本是一样的。

刚刚在“工作档案”这个工作方法上，找到了一种新的对齐方法：
1. 先把 Markdown 转成 HTML；
2. 根据 HTML 去纠正叙事方向；
3. 再反过来修改 Markdown

对，这样就可以用 HTML 更可读的方式来展示这里面的信息结构。如果发现信息结构不对，再返回去把 Markdown 里面的信息内容修改。如果想进一步结构化，就可以把 JSON 提取出来。

 